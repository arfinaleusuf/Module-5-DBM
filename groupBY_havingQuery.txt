GROUP BY, HAVING FUMCTIONS

GROUP by --> department wise
            SELECT DEPARTMENT_ID, MAX(SALARY)
            FROM EMPLOYEES
            GROUP BY DEPARTMENT_ID;  --- DEPARTMENT E JOTO JON ACHE OI NUMBER TA SHOW KORE

HAVING ---> GROUP FUMCTIONS ER UPOR APPLIED CONDITIONS  (WHERE DIFFERANT)

WHERE HOCCHE OVERALL EKTA CONDITION
HAVING HOCCHE GROUP FUNCTION ER UPOR EKTA CONDITION

SELECT DEPARTMENT_ID, MAX(SALARY)
FROM EMPLOYEES
WHERE DEPARTMENT_ID != 20  -- eta table er upor kaj korche tai where e diyechi
GROUP BY DEPARTMENT_ID
HAVING MAX(SALARY) > 5000  -- eta DEPARTMENT_ID er upor kaj korche ja GROUP FUNCTION tai HAVING er moddhe deya hoyeche
